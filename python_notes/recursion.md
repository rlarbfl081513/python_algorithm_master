# 재귀함수 return
좋은 지점 집으셨어요 👍
재귀에서 `return node(num//2)`에 **`return`을 붙이냐/안 붙이냐**는 큰 차이가 있습니다.

---

## 1. 지금 코드 구조

```python
def node(num):
    global result, pq
    if num == 1:
        return result
    else:
        result += pq[num//2]
        return node(num//2)   # ← 여기!
```

* `node(n)`이 호출되면, `num`이 1이 될 때까지 계속 재귀 호출
* `return node(num//2)`를 쓰면, 마지막에 `result`가 리턴될 때까지 **계속 상위 호출로 값을 되돌려줌**
* 즉, **최종적으로 `print(node(n))`에 값이 제대로 전달됨**

---

## 2. 만약 `return`을 빼면?

```python
def node(num):
    global result, pq
    if num == 1:
        return result
    else:
        result += pq[num//2]
        node(num//2)   # ← return 없음
```

* `node()` 함수는 중간에 재귀를 불러도 **아무 값도 반환하지 않음**
* 그래서 최상위에서 `print(node(n))` 하면 `None`이 나옴
  (재귀 호출은 잘 되지만, 결과를 함수 밖으로 전달하지 못하기 때문)

---

## 3. 차이 요약

* `return node(...)`
  → **재귀 결과를 반환값으로 돌려줌** → 최종 결과를 받을 수 있음
* `node(...)`
  → **재귀 호출만 실행하고 결과를 무시** → 함수는 `None` 반환

---

## 4. 간단한 예시

```python
def f(x):
    if x == 0:
        return 0
    else:
        return f(x-1) + 1   # return 붙임

print(f(5))  # 5

def g(x):
    if x == 0:
        return 0
    else:
        g(x-1)   # return 없음

print(g(5))  # None
```

---

✅ 결론

* **계산 결과를 밖에서 쓰고 싶으면 `return` 필수**
* 단순히 “안에서 동작만 하게 하고, 결과값은 필요 없는 재귀”라면 `return` 생략 가능

---
